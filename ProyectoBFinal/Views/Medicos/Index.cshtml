@model IEnumerable<ProyectoBFinal.Models.Medicos>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de medicos</h2>
@using (Html.BeginForm())
{
    <div class="container-fluid">
        <div class="mt-3 col-md-4 col-md-offset-8 input-group">
            @Html.TextBox("SearchString", null, null, new { @class = "form-control" })
            <span class="input-group-btn">
                <input type="submit" class="btn btn-default" value="Buscar" />
            </span>
        </div>
    </div>
}
<br />
<p>

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Exequatur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Especialidad)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Exequatur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Especialidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
